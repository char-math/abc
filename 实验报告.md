# 计算机系统实验报告

## 实验环境

- **操作系统**：Linux
- **编译器**：g++ 
- **编译选项**：-O3
- **编程语言**：C++

## 实验一 Transformer 基础算子实现

### 1 实验目的
1. 通过微调 LLaMA 模型，使其在特定任务（如文本分类、问答系统、文本生成等）上表现更优。
2. 探索不同微调策略对模型性能的影响。
3. 验证微调后的模型在实际应用中的效果。

本次实验旨在实现深度学习模型推理中的三个核心算子：Softmax、矩阵乘法（MatMul）和均方根归一化（RMSNorm）。这些算子是Transformer架构模型的基础组件，正确高效的实现对于模型推理性能至关重要。

### 2 算法实现

#### 2.1 Softmax函数

Softmax函数将输入向量转换为概率分布，公式如下：

$$\text{softmax}(x_i) = \frac{e^{x_i}}{\sum_{j=1}^{N} e^{x_j}}$$

**数值稳定性优化**：为防止指数运算溢出，采用max-subtraction技巧：

$$\text{softmax}(x_i) = \frac{e^{x_i - \max(x)}}{\sum_{j=1}^{N} e^{x_j - \max(x)}}$$

```cpp
void CBackend::softmax(float* x, int n) {
    float max_val = x[0];
    for (int i = 1; i < n; ++i) {
        if (x[i] > max_val) max_val = x[i];
    }
    
    float sum = 0.0f;
    for (int i = 0; i < n; ++i) {
        x[i] = expf(x[i] - max_val);
        sum += x[i];
    }
    
    float inv_sum = 1.0f / sum;
    for (int i = 0; i < n; ++i) {
        x[i] *= inv_sum;
    }
}
```
**设计要点**：
- 第一步找出最大值用于数值稳定
- 计算指数和时减去最大值
- 使用乘法替代除法提升性能

#### 2.2 矩阵乘法（MatMul）

计算矩阵与向量的乘积：

$$o(d,1) = W(d,n) \times x(n,1)$$

其中W是d×n的矩阵，按行优先方式存储。

```cpp
void CBackend::matmul(float* o, float* x, float* w, int n, int d) {
    for (int i = 0; i < d; ++i) {
        float sum = 0.0f;
        float* w_row = w + i * n;
        for (int j = 0; j < n; ++j) {
            sum += w_row[j] * x[j];
        }
        o[i] = sum;
    }
}
```
**设计要点**：
- 外层循环遍历输出维度
- 每行与输入向量做点积
- 使用指针减少数组索引开销

#### 2.3 均方根归一化（RMSNorm）

RMSNorm的计算公式：

$$\bar{x}_i = \frac{x_i}{\sqrt{\frac{1}{n}\sum_{j=1}^{n}x_j^2 + \epsilon}}$$

$$y_i = \bar{x}_i \times w_i$$

其中ε=1e-5用于数值稳定性。
```cpp
void CBackend::rmsnorm(float* y, float* x, float* w, int n) {
    float ss = 0.0f;
    for (int i = 0; i < n; ++i) {
        ss += x[i] * x[i];
    }
    
    float inv_rms = 1.0f / sqrtf(ss / n + 1e-5f);
    
    for (int i = 0; i < n; ++i) {
        y[i] = x[i] * inv_rms * w[i];
    }
}
```
**设计要点**：
- 先计算平方和
- 计算均方根的倒数避免重复除法
- 同时对x进行归一化并应用缩放参数

## 实验二 LLM 推理优化 OpenMP|SIMD
### 1 实验目的

1. 理解 OpenMP 并行编程模型的原理和使用方法
2. 掌握 SIMD 向量化优化技术
3. 学习使用 OpenBLAS 高性能数学库
4. 对比不同优化方法的性能差异

### 2 实验内容

在原有 CBackend 类的基础上，创建四个子类，分别实现不同的优化版本：

| 子类 | 优化方式 |
|------|----------|
| OMPBackend | OpenMP 并行 |
| SIMDBackend | AVX2 SIMD 向量化 |
| OPTBackend | OpenMP + SIMD 混合 |
| OBLASBackend | OpenBLAS 数学库 |

每个子类重写 `softmax`、`matmul`、`rmsnorm` 三个核心函数。

### 3 核心代码实现

#### 3.1 基类声明（backend.hpp）

```cpp
#ifndef BACKEND_HPP
#define BACKEND_HPP

class CBackend {
public:
    virtual void softmax(float* x, int size);
    virtual void matmul(float* xout, float* x, float* w, int n, int d);
    virtual void rmsnorm(float* o, float* x, float* weight, int size);
    void axpy(float *y, float *x, float factor, int dim);
    void dot(float *y, float *x1, float*x2, int dim);
    void ropeEncoding(float *q, float *k, int headSize, int position, int dim, int kvDim);
    void gemvQkSeq(float *q, float *key, float *att, int pos, int kvDim, int headSize);
    void weightedV(float *xb, float *value, float *att, int pos, int kvDim, int headSize);
    void swiGLLUFunc(float *hb, float *hb2, int hiddenDim);
    CBackend();
    virtual ~CBackend();
};

class OMPBackend : public CBackend {
public:
    OMPBackend();
    ~OMPBackend();
    void softmax(float* x, int size) override;
    void matmul(float* xout, float* x, float* w, int n, int d) override;
    void rmsnorm(float* o, float* x, float* weight, int size) override;
};

class SIMDBackend : public CBackend {
public:
    SIMDBackend();
    ~SIMDBackend();
    void softmax(float* x, int size) override;
    void matmul(float* xout, float* x, float* w, int n, int d) override;
    void rmsnorm(float* o, float* x, float* weight, int size) override;
};

class OPTBackend : public CBackend {
public:
    OPTBackend();
    ~OPTBackend();
    void softmax(float* x, int size) override;
    void matmul(float* xout, float* x, float* w, int n, int d) override;
    void rmsnorm(float* o, float* x, float* weight, int size) override;
};

class OBLASBackend : public CBackend {
public:
    OBLASBackend();
    ~OBLASBackend();
    void softmax(float* x, int size) override;
    void matmul(float* xout, float* x, float* w, int n, int d) override;
    void rmsnorm(float* o, float* x, float* weight, int size) override;
};

#endif
```

#### 3.2 原始版本（CBackend）
见实验一

#### 3.3 OpenMP 并行优化（OMPBackend）

使用 `#pragma omp parallel for` 将循环分配到多个线程并行执行。

```cpp
void OMPBackend::softmax(float* x, int n) {
    // 找最大值 - 使用 reduction(max)
    float max_val = x[0];
    #pragma omp parallel for reduction(max:max_val)
    for (int i = 1; i < n; ++i) {
        if (x[i] > max_val) max_val = x[i];
    }
    
    // 计算 exp 和求和 - 使用 reduction(+)
    float sum = 0.0f;
    #pragma omp parallel for reduction(+:sum)
    for (int i = 0; i < n; ++i) {
        x[i] = expf(x[i] - max_val);
        sum += x[i];
    }
    
    // 归一化 - 无依赖，直接并行
    sum = 1.0f / sum;
    #pragma omp parallel for
    for (int i = 0; i < n; ++i) {
        x[i] *= sum;
    }
}

void OMPBackend::matmul(float* o, float* x, float* w, int n, int d) {
    #pragma omp parallel for
    for (int i = 0; i < d; ++i) {
        float sum = 0.0f;
        float* w_row = w + i * n;
        for (int j = 0; j < n; ++j) {
            sum += w_row[j] * x[j];
        }
        o[i] = sum;
    }
}

void OMPBackend::rmsnorm(float* y, float* x, float* w, int n) {
    float ss = 0.0f;
    #pragma omp parallel for reduction(+:ss)
    for (int i = 0; i < n; ++i) {
        ss += x[i] * x[i];
    }
    
    float inv_rms = 1.0f / sqrtf(ss / n + 1e-5f);
    
    #pragma omp parallel for
    for (int i = 0; i < n; ++i) {
        y[i] = x[i] * inv_rms * w[i];
    }
}
```

#### 3.4 SIMD 向量化优化（SIMDBackend）

使用 AVX2 指令集，一次处理 8 个 float。

```cpp
void SIMDBackend::matmul(float* o, float* x, float* w, int n, int d) {
    for (int i = 0; i < d; ++i) {
        __m256 sum_vec = _mm256_setzero_ps();
        float* w_row = w + i * n;
        int j = 0;
        
        // 每次处理 8 个元素
        for (; j <= n - 8; j += 8) {
            __m256 x_vec = _mm256_loadu_ps(&x[j]);
            __m256 w_vec = _mm256_loadu_ps(&w_row[j]);
            sum_vec = _mm256_fmadd_ps(x_vec, w_vec, sum_vec);
        }
        
        // 规约求和
        float sum_arr[8];
        _mm256_storeu_ps(sum_arr, sum_vec);
        float sum = 0.0f;
        for (int k = 0; k < 8; k++) sum += sum_arr[k];
        
        // 处理剩余元素
        for (; j < n; ++j) {
            sum += w_row[j] * x[j];
        }
        o[i] = sum;
    }
}

void SIMDBackend::rmsnorm(float* y, float* x, float* w, int n) {
    // 向量化计算平方和
    __m256 sum_vec = _mm256_setzero_ps();
    int i = 0;
    for (; i <= n - 8; i += 8) {
        __m256 x_vec = _mm256_loadu_ps(&x[i]);
        __m256 x2_vec = _mm256_mul_ps(x_vec, x_vec);
        sum_vec = _mm256_add_ps(sum_vec, x2_vec);
    }
    
    // 规约
    float ss_arr[8];
    _mm256_storeu_ps(ss_arr, sum_vec);
    float ss = 0.0f;
    for (int k = 0; k < 8; k++) ss += ss_arr[k];
    for (; i < n; ++i) ss += x[i] * x[i];
    
    float inv_rms = 1.0f / sqrtf(ss / n + 1e-5f);
    __m256 inv_rms_vec = _mm256_set1_ps(inv_rms);
    
    // 向量化归一化
    for (i = 0; i <= n - 8; i += 8) {
        __m256 x_vec = _mm256_loadu_ps(&x[i]);
        __m256 w_vec = _mm256_loadu_ps(&w[i]);
        __m256 result = _mm256_mul_ps(_mm256_mul_ps(x_vec, inv_rms_vec), w_vec);
        _mm256_storeu_ps(&y[i], result);
    }
    for (; i < n; ++i) {
        y[i] = x[i] * inv_rms * w[i];
    }
}
```

#### 3.5 OpenMP+SIMD 混合优化（OPTBackend）

结合线程级并行和指令级并行。

```cpp
void OPTBackend::matmul(float* o, float* x, float* w, int n, int d) {
    #pragma omp parallel for
    for (int i = 0; i < d; ++i) {
        __m256 sum_vec = _mm256_setzero_ps();
        float* w_row = w + i * n;
        int j = 0;
        for (; j <= n - 8; j += 8) {
            __m256 x_vec = _mm256_loadu_ps(&x[j]);
            __m256 w_vec = _mm256_loadu_ps(&w_row[j]);
            sum_vec = _mm256_fmadd_ps(x_vec, w_vec, sum_vec);
        }
        float sum_arr[8];
        _mm256_storeu_ps(sum_arr, sum_vec);
        float sum = 0.0f;
        for (int k = 0; k < 8; k++) sum += sum_arr[k];
        for (; j < n; ++j) {
            sum += w_row[j] * x[j];
        }
        o[i] = sum;
    }
}
```

#### 3.6 OpenBLAS 优化（OBLASBackend）

调用高度优化的 BLAS 库。

```cpp
void OBLASBackend::matmul(float* o, float* x, float* w, int n, int d) {
    cblas_sgemv(CblasRowMajor, CblasNoTrans, d, n, 
                1.0f, w, n, x, 1, 0.0f, o, 1);
}
```


### 4 实验结果

#### 4.1 测试命令

```bash
# 原始版本
./muduo data/stories15M.bin data/tokenizer.bin "once upon a time," llama cpu

# OpenMP版本
./muduo data/stories15M.bin data/tokenizer.bin "once upon a time," llama cpu_x86

# SIMD版本
./muduo data/stories15M.bin data/tokenizer.bin "once upon a time," llama cpu_arm

# 混合版本
./muduo data/stories15M.bin data/tokenizer.bin "once upon a time," llama cpu_opt

# OpenBLAS版本
./muduo data/stories15M.bin data/tokenizer.bin "once upon a time," llama openblas
```

#### 4.2 性能对比

| Backend | 优化方式 | 吞吐量 (token/s) | 加速比 |
|---------|----------|-----------------|--------|
| CBackend | 原始串行 | 38.0 | 1.00x |
| SIMDBackend | SIMD 向量化 | 175.7 | 4.62x |
| OMPBackend | OpenMP 并行 | 217.4 | 5.72x |
| OPTBackend | OpenMP + SIMD | 359.7 | 9.47x |
| OBLASBackend | OpenBLAS 库 | 579.1 | 15.24x |

#### 4.3 实验总结

1. **并行化效果显著**：OpenMP 和 SIMD 都能有效提升推理性能，分别获得 5.7 倍和 4.6 倍加速

2. **混合优化最佳**：OpenMP + SIMD 结合使用，获得 9.5 倍加速，说明两种技术正交互补

3. **专业库优势明显**：OpenBLAS 经过深度优化，性能达到 15.2 倍，远超手写实现

4. **虚函数的重要性**：多态必须将基类函数声明为 virtual，否则子类函数不会被调用

通过本次实验，深入理解了：
- OpenMP 并行编程模型的 fork-join 模式
- SIMD 向量化编程的 AVX2 指令集使用
- reduction 子句在并行归约中的重要作用
- 不同优化技术的性能特点和适用场景
- 高性能计算中多级并行的叠加效果

## 实验三 LLM量化

### 1 实验目的

1. 理解深度学习模型的INT8量化原理
2. 实现矩阵乘法的INT8量化版本（matmulQuant）
3. 实现模型的INT8量化导出和加载功能
4. 掌握局部缩放因子和全局缩放因子的区别与应用

### 2 实验原理

#### 2.1 INT8量化

INT8量化是将32位浮点数（FP32）转换为8位整数（INT8）的过程，可以显著减少模型存储空间和加速推理计算。

量化公式：
```
q = round(x / scale)
x_reconstructed = q * scale
```

其中：
- x：原始FP32值
- q：量化后的INT8值
- scale：缩放因子，计算公式为 `scale = max_abs / 127`

### 2.2 全局缩放因子 vs 局部缩放因子

- **全局缩放因子**：所有权重共享同一个scale值
- **局部缩放因子**：每个权重矩阵（wq, wk, wv, wo, w1, w2, w3）拥有独立的scale值

局部缩放因子能获得更高的量化精度，因为每个矩阵的动态范围不同。

## 3 实验内容

### 3.1 实现matmulQuant函数（backendQuant.cpp）
全局：
```cpp
void CBackendQuant::matmulQuant(float* o, float* x, float* w_quant, float* scale,
    int n, int d) {
    int8_t* w_quant_int8 = reinterpret_cast<int8_t*>(w_quant);
    float s = scale[0];  // 只取第一个值
    
    for (int i = 0; i < d; i++) {
        float sum = 0.0f;
        for (int j = 0; j < n; j++) {
            sum += (float)w_quant_int8[i * n + j] * s * x[j];
        }
        o[i] = sum;
    }
}
```
局部：
```cpp
void CBackendQuant::matmulQuant(float* o, float* x, float* w_quant, float* scale, int n, int d) { 
    int8_t* w_quant_int8 = reinterpret_cast<int8_t*>(w_quant);
    
    for (int i = 0; i < d; i++) {
        float sum = 0.0f;
        float s = scale[i];
        
        for (int j = 0; j < n; j++) {
            sum += (float)w_quant_int8[i * n + j] * s * x[j];
        }
        o[i] = sum;
    }
}
```
- 将float指针转换为int8_t指针以正确读取量化数据
- 使用scale[i]实现每行独立的缩放因子
- 先反量化再执行矩阵-向量乘法

### 3.2 实现computeGlobalScale函数（transformerQuant.cpp）

```cpp
void computeGlobalScale(float* start1, float* end1, float* start2, float* end2, float& scale) {
    float max_abs = 0.0f;
    
    for (float* p = start1; p < end1; p++) {
        float abs_val = fabsf(*p);
        if (abs_val > max_abs) max_abs = abs_val;
    }
    
    for (float* p = start2; p < end2; p++) {
        float abs_val = fabsf(*p);
        if (abs_val > max_abs) max_abs = abs_val;
    }
    
    if (max_abs < 1e-8f) {
        scale = 1.0f;
    } else {
        scale = max_abs / 127.0f;
    }
}
```

### 3.3 实现quantizeToInt8函数（transformerQuant.cpp）

```cpp
void quantizeToInt8(float* src, int8_t* dst, size_t size, float scale) {
    if (scale < 1e-8f) {
        memset(dst, 0, size * sizeof(int8_t));
        return;
    }
    
    float inv_scale = 1.0f / scale;
    
    for (size_t i = 0; i < size; i++) {
        float quantized_float = src[i] * inv_scale;
        int quantized_int = (int)roundf(quantized_float);
        
        if (quantized_int > 127) {
            quantized_int = 127;
        } else if (quantized_int < -128) {
            quantized_int = -128;
        }
        
        dst[i] = (int8_t)quantized_int;
    }
}
```

### 3.4 实现局部缩放因子导出（exportQuantizedModel）

```cpp
void CTransformerQuant::exportQuantizedModel(CModelConfig* modelConfig, float* ptr, 
                                              int sharedWeights, const std::string& outputFile) {
    // ... 写入配置和FP32权重 ...
    
    // 对每个权重分别计算scale并量化
    auto quantizeAndWrite = [&outFile](float* data, size_t size) -> float {
        float max_abs = 0.0f;
        for (size_t i = 0; i < size; i++) {
            float abs_val = fabsf(data[i]);
            if (abs_val > max_abs) max_abs = abs_val;
        }
        float scale = max_abs / 127.0f;
        
        outFile.write(reinterpret_cast<char*>(&scale), sizeof(float));
        // ... 量化并写入INT8数据 ...
        return scale;
    };
    
    quantizeAndWrite(wq_start, wq_size);
    quantizeAndWrite(wk_start, wk_size);
    // ... 其他权重 ...
}
```

### 3.5 实现量化模型加载（mapWeightsToMemoryQuant）

```cpp
void CTransformerQuant::mapWeightsToMemoryQuant(CModelConfig* modelConfig, float* ptr, int sharedWeights){
    // ... 读取FP32权重 ...
    
    // 读取每个权重的scale数组和INT8数据
    scale_wq = currentPtr;
    currentPtr += numLayers;
    w.wq = currentPtr;
    currentPtr += (numLayers * modelConfig->dim * modelConfig->dim) / 4;
    
    // ... 其他权重 ...
}
```

### 4 编译与运行

#### 4.1 导出量化模型
```bash
make clean
make
./muduo /path/to/stories15M.bin /path/to/tokenizer.bin --quantize export /path/to/int8_local.bin
```

#### 4.2 运行量化模型推理

```bash
./muduo /path/to/int8_local.bin /path/to/tokenizer.bin --quantize load
```

### 5 实验结果


#### 5.1 性能对比

| 模型类型 | 文件大小 | 推理速度 |
|---------|---------|---------|
| FP32模型 | ~60MB | 98 token/s |
| INT8量化模型 | ~15MB | 85 token/s |

#### 5.2 问题与解决方案

（1）Segmentation Fault问题

**问题**：加载量化模型时出现段错误

**原因**：
- scale指针越界访问
- 全局scale和局部scale混用

**解决方案**：
- 使用scale[0]适配全局scale模型
- 修改mapWeightsToMemoryQuant正确读取scale数组

（2）输出为<unk>问题

**问题**：推理输出全是<unk>

**原因**：scale值没有正确读取，导致反量化后的权重值错误

**解决方案**：确保导出和加载时scale的存储顺序一致

### 6 实验总结

本次实验成功实现了：
1. INT8量化的核心算子matmulQuant
2. 全局缩放因子和局部缩放因子的计算
3. 模型的量化导出和加载功能

通过实验加深了对模型量化的理解：
- 量化能有效减少模型大小（约4倍）
- 局部缩放因子比全局缩放因子精度更高
- 需要确保导出和加载的格式一致

## 实验四 LLM推理优化 KVCache

### 1 实验目的

1. 实现Transformer模型的无KV Cache版本前向推理函数
2. 对比分析KV Cache版本的性能优势
3. 理解KV Cache在大语言模型推理中的优化原理

### 2 实验原理

#### 2.1 KV Cache机制

在Transformer的自回归生成过程中，每次生成一个新token时，需要计算当前token与之前所有token的注意力分数。标准实现中，每次都会重新计算所有历史token的Key和Value矩阵，导致大量重复计算。

KV Cache的核心思想：将历史token的K和V矩阵缓存起来，每次只计算新token的K和V，然后与缓存的K、V拼接后进行注意力计算。

#### 2.2 计算冗余分析

**无KV Cache版本（forwardWithoutKVCache）：**
- 对于长度为L的序列，每层需要计算L次K和L次V
- 总计算量：O(L² × d)（d为维度）
- 时间复杂度：O(L²)

**有KV Cache版本（forward）：**
- 只计算新token的K和V（1次）
- 从缓存中读取历史K、V
- 总计算量：O(L × d)
- 时间复杂度：O(L)

### 三、实现细节

#### 3.1 无KV Cache版本核心代码

```cpp
// 为每个token独立计算K、V
for (int i = 0; i < len; i++) {
    backend->matmul(state->k + i*kvDim, 
                    state->branchActivation + i*dim, 
                    w.wk + layer * dim * kvDim, 
                    dim, kvDim);
    backend->matmul(state->v + i*kvDim, 
                    state->branchActivation + i*dim, 
                    w.wv + layer * dim * kvDim, 
                    dim, kvDim);
}
```

#### 3.2 有KV Cache版本核心代码

```cpp
// 只计算当前token的K、V
state->k = state->keyCache + kvCacheOffset + pos * kvDim;
state->v = state->valueCache + kvCacheOffset + pos * kvDim;
backend->matmul(state->k, state->branchActivation, 
                w.wk + layer * dim * kvDim, dim, kvDim);
backend->matmul(state->v, state->branchActivation, 
                w.wv + layer * dim * kvDim, dim, kvDim);
```

#### 3.3 编译运行

```bash
make clean
make
./muduo src/data/stories15M.bin src/data/tokenizer.bin --openKvCache 0
```
### 4 KV Cache优化策略分析

#### 4.1 优化原理

1. **消除重复计算**：历史token的K、V只需计算一次
2. **注意力计算优化**：使用cache的K、V直接计算注意力分数
3. **内存布局优化**：连续存储cache数据，提高缓存命中率

#### 4.2 内存开销分析

KV Cache的内存开销：
```
Memory = 2 × num_layers × max_seq_len × kv_dim × sizeof(float)
```

对于stories15M模型：
- num_layers = 12
- kv_dim = 128
- max_seq_len = 512
- 内存 = 2 × 12 × 512 × 128 × 4 = 6.29 MB


### 5 实验总结

1. KV Cache可以显著提升自回归推理性能，长序列下加速比可达2000倍以上
2. 计算冗余主要来自于重复的K、V矩阵乘法
3. 以少量内存开销（6MB/512长度）换取巨大性能提升是值得的

